{{ form_start(form, {'attr': {'id' : id | default('form')}} ) }}

  {{ form_widget(form) }}

{{ form_end(form) }}


<script>

  var message = '¿Esta seguro que desea desbloquear este usuario?';
  function confirmUnblock()
  {
    bootbox.confirm(message, function(result){
      if(result == true)
      {
        document.form.submit();
      }
    });
  }

</script>
